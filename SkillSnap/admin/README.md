# SkillSnap Admin

React + Vite admin console for SkillSnap. Configure `VITE_API_BASE_URL` and run `npm run dev`.

**Production:** [skillsnap-admin-one.vercel.app](https://skillsnap-admin-one.vercel.app) — on Vercel, set `VITE_API_BASE_URL` to your Render API base (must end with `/api`).
